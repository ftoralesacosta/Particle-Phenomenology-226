#include <iostream>
#include <TCanvas.h>
#include <TGraph.h>
#include <fstream>


using namespace std;

void EMShower()
{

  vector <Float_t> Electrons (1000,1000);//working in MeV, 1000 Mev = 1GeV

    for (int dX = 1; dX <= 230;) //working in mm
      {
	cout<<"Hello World!";

      }








  TCanvas * c1 = new TCanvas();

  TH1D * h1 = new TH1D("h1","Analog Latchup Heights",100,0,500);
  for (int n=0; n < Analogs.size(); n++)
    {
      h1->Fill(Analogs[n]);
    }
  h1->Draw();
}


